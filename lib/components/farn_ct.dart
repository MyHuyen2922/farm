import 'package:flutter/material.dart';

class FarmCanTho extends StatelessWidget {
  const FarmCanTho({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Center(child: Text("Cần Thơ")),
      ),
      body: Container(
        child: Center(child: Text("BBBBB")),
      ),
    );
  }
}
