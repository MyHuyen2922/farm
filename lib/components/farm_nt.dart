import 'package:flutter/material.dart';

class FarmNinhThuan extends StatelessWidget {
  const FarmNinhThuan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Center(child: Text("Ninh Thuận")),
      ),
      body: Container(
        child: Center(child: Text("AAA")),
      ),
    );
  }
}
